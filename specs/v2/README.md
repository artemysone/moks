# Inherited OpenCode rewrite notes

These files are from OpenCode's unfinished Session rewrite. They are not a moks product plan. They are not a v1 vs v2 hiring roadmap.

What ships is the CLI/TUI loop (`SessionPrompt.loop` in `packages/cli/src/session`). Engine Session (`/api/session`, code name `SessionV2`) is mounted and incomplete. Do not pick up work from this folder unless you are already in that engine code.

See root `AGENTS.md`.
