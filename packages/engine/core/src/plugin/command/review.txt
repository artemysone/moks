Review the local hiring packet before commit/push.

You are reviewing a candidate/req packet — not a git PR. Do not lead with `git diff`, `git show`, or `gh pr`. Default path is packet review of HIRING.md and candidates/.

Input: $ARGUMENTS

Worktree: `${path}`

## What to review

Resolve packet materials in order:

1. Paths or focus from `$ARGUMENTS` / attachments
2. Focused packet: that req's `HIRING.md`, `candidates/<id>.md` (env Focused req / Candidates). Never review or create company-root `candidates/` when reqs are subdirectories.
3. Session-visible local hiring file changes (Modified Files / session diffs of HIRING.md and candidate cards)
4. Pending commits via `moks status` when disposition readiness matters

Read the full files you will cite. Use skills to deepen analysis when needed:

- **score-candidate** — fit vs HIRING.md with evidence; score lives on the card
- **commit-disposition** — disposition consistency and readiness to commit
- **req-context** / **draft-outreach** when HIRING.md gaps or outreach quality is in scope

## Structure the review

1. **Fit vs scorecard** — dimension scores, must-haves, deal-breakers; cite card / HIRING.md
2. **Evidence gaps** — missing materials, weak claims, unverified bar items
3. **Outreach quality** — if drafts exist on the card: accuracy, tone, overclaims; never send
4. **Disposition consistency** — recommendation matches evidence; ready for `moks commit` / risks before `moks push`
5. **Risks before push** — adverse actions need confirm; incomplete packet; conflicting notes

## Decision verbs

- Inspect: `moks status` / `moks diff`
- Record intent: `moks commit`
- Human only: `moks review` then `moks push` (adverse reject/offer/hire need confirm)

Prefer the native `commit` / `status` / `diff` tools when checking readiness. Do not use raw `git commit`. Do not call push or review from the agent.

## Output

- Matter-of-fact, scannable bullets or short sections
- Severity-aware: block-push risks first, then gaps, then polish
- No flattery; no invented resume facts
- End with a clear next step (fill gap / re-score / commit disposition / push)
