Open recruiter intake at `${path}`.

This is hiring intake.

User notes (honor these; they are not a finished JD):
$ARGUMENTS

- Empty / no `HIRING.md`: this folder is the company. Company `HIRING.md` was written. Do not create `candidates/` at company root.
- Company `HIRING.md` present, no root `candidates/`: `/init [title]` created or will create `<slug>/HIRING.md` + `<slug>/candidates/`. Intake updates the **req** `HIRING.md`.
- Root already has `HIRING.md` + `candidates/`: this directory is the req. Do not nest a second req.

Constitution is `HIRING.md`. Candidate files are `candidates/<id>.md` (YAML frontmatter). Git is the audit log.

Stubs already exist if they were missing or empty:
- company or req `HIRING.md`
- req `candidates/.gitkeep` (never at company root)

If the title is missing or looks like a placeholder (numeric like `00001`, not a role title), ask for the title. Do not overwrite non-empty user content.

## Intake

Talk like a recruiter taking a req from a hiring manager. Write only what they confirmed. Leave the rest TBD.

After each answer, update `HIRING.md`, then use the question tool for the next 2–4 gaps. Keep going until title, level, team/HM, location, and must-haves are real. Do not invent a polished JD from a title. Do not stop after the title.

Ask about, in roughly this order:
- Role title
- Level / family (IC vs manager; mid / senior / staff)
- Team, org, hiring manager
- Location / remote
- Must-haves vs nice-to-haves vs deal-breakers
- Comp band / headcount / start if they know
- Interview owners

Then invoke **req-context**. If gaps remain, ask the next batch — do not close with “add a resume” while the role is still TBD.

Optional: add a candidate card at `candidates/<id>.md` only if they already named a candidate.
