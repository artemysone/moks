export const DEFAULT_PLACEHOLDERS = {
  normal: ["Score this resume against the req", "Draft outreach for the shortlist", "Open a req with /open-req"],
  shell: ["moks status", "ls candidates", "pwd"],
}
